/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // Keep defaults; you can toggle features as Next evolves
  }
};

export default nextConfig;
